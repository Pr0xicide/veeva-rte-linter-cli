const { GRADE } = require('../util/logging')
const { TYPES } = require('../util/token-types')

const checkUniqueTokens = () => {}

const validate = (tokens) => {
  const logs = []
  return logs
}

module.exports = {
  validate,
}
